<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.saas.cloud.rbac.mapper.DeptMapper">

    <!-- 批量更新子部门的 ancestors：将旧前缀替换为新前缀 -->
    <update id="updateChildAncestors">
        UPDATE sys_dept
        SET ancestors = CONCAT(#{newAncestors}, SUBSTRING(ancestors, LENGTH(#{oldAncestors}) + 1))
        WHERE ancestors LIKE CONCAT(#{oldAncestors}, ',%')
          AND delete_flag = 0
    </update>

</mapper>
